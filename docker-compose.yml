services:
  postgres:
    image: postgres:16
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: uptrack
      POSTGRES_PASSWORD: uptrack
      POSTGRES_DB: uptrack
    volumes:
      - uptrack_pgdata:/var/lib/postgresql/data

volumes:
  uptrack_pgdata:
